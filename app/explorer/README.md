# Cannabiverse Query Explorer

The explorer is a standalone consumer of the governed graph.

It supports:
- structured scientific filtering,
- independent identity and occurrence thresholds,
- query receipts,
- CSV/JSON result export,
- graph traversal,
- compound comparison,
- explicit shortest-path tracing.

The explorer may select or visualize records. It must never upgrade the authority of the underlying data.
